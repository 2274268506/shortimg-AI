# TC-GO 项目结构

```
TC-GO/
├── backend/                          # Go 后端
│   ├── main.go                      # 入口文件
│   ├── config/                      # 配置管理
│   │   └── config.go
│   ├── database/                    # 数据库连接
│   │   └── database.go
│   ├── models/                      # 数据模型
│   │   ├── album.go                # 相册模型
│   │   └── image.go                # (隐式在 controllers 中)
│   ├── controllers/                 # 控制器层
│   │   ├── album_controller.go     # 相册 CRUD
│   │   └── image_controller.go     # 图片 CRUD (474 行)
│   ├── routes/                      # 路由配置
│   │   └── routes.go
│   └── utils/                       # 工具函数
│       └── image.go                # 图片处理工具
│
└── frontend/                         # Vue3 前端
    ├── index.html
    ├── package.json
    ├── vite.config.js
    ├── public/
    └── src/
        ├── main.js                  # 应用入口
        ├── App.vue                  # 根组件
        │
        ├── api/                     # API 接口层
        │   └── index.js            # 封装所有 API 调用
        │
        ├── router/                  # 路由配置
        │   └── index.js
        │
        ├── stores/                  # Pinia 状态管理
        │   └── image.js            # 图片和相册状态 (169 行)
        │
        ├── utils/                   # 工具函数
        │   └── request.js          # Axios 封装
        │
        ├── composables/             # ✨ 组合式函数 (新增)
        │   └── useImageOperations.js   # 图片操作逻辑 (~80 行)
        │
        ├── components/              # ✨ 可复用组件 (新增)
        │   ├── ImageToolbar.vue         # 顶部工具栏 (~50 行)
        │   ├── AlbumSidebar.vue         # 相册侧边栏 (~140 行)
        │   ├── StatsPanel.vue           # 统计面板 (~80 行)
        │   ├── ImageGrid.vue            # 网格视图 (~150 行)
        │   ├── ImageList.vue            # 列表视图 (~120 行)
        │   ├── UploadDialog.vue         # 上传对话框 (~110 行)
        │   ├── AlbumDialog.vue          # 相册对话框 (~90 行)
        │   └── ImagePreviewDialog.vue   # 预览对话框 (~150 行)
        │
        └── views/                   # 页面视图
            ├── ImageManager.vue         # ✨ 主页面 (重构: 1292→330 行)
            └── ImageManager.vue.backup  # 原始文件备份
```

## 组件依赖关系图

```
ImageManager.vue (主容器)
├─ ImageToolbar.vue
│  ├─ 事件: @upload → 打开上传对话框
│  └─ 事件: @createAlbum → 打开创建相册对话框
│
├─ AlbumSidebar.vue
│  ├─ 事件: @selectAlbum → 切换当前相册
│  ├─ 事件: @editAlbum → 编辑相册
│  └─ 事件: @deleteAlbum → 删除相册
│
├─ StatsPanel.vue
│  └─ 仅展示，无交互
│
├─ ImageGrid.vue (网格视图)
│  ├─ 事件: @preview → 预览图片
│  ├─ 事件: @copyLink → 复制链接
│  ├─ 事件: @download → 下载图片
│  └─ 事件: @delete → 删除图片
│
├─ ImageList.vue (列表视图)
│  ├─ 事件: @preview → 预览图片
│  ├─ 事件: @copyLink → 复制链接
│  ├─ 事件: @download → 下载图片
│  └─ 事件: @delete → 删除图片
│
├─ UploadDialog.vue
│  └─ 事件: @upload → 执行上传操作
│
├─ AlbumDialog.vue
│  └─ 事件: @submit → 提交表单（创建/编辑）
│
└─ ImagePreviewDialog.vue
   ├─ 事件: @copyLink → 复制链接
   └─ 事件: @download → 下载图片
```

## 数据流向

```
User Action
    ↓
Component Event
    ↓
ImageManager (事件处理)
    ↓
Pinia Store (状态管理)
    ↓
API Layer (axios)
    ↓
Backend API
    ↓
Database
    ↓
Response
    ↓
Store Update
    ↓
Component Re-render
```

## 重构亮点

### 🎯 单一职责原则
每个组件只负责一个功能模块，易于理解和维护。

### 🔄 可复用性
组件可以在其他项目或页面中独立使用。

### 🧩 组合式 API
使用 `composables` 提取共享逻辑，符合 Vue 3 最佳实践。

### 📦 模块化
清晰的目录结构，便于团队协作和代码审查。

### 🧪 可测试性
小组件更容易编写单元测试和集成测试。

### 🚀 性能优化
- 更细粒度的组件更新
- 按需加载组件的可能性
- 更好的 Tree-shaking 支持
